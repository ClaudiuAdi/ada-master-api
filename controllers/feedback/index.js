const giveFeedback = require('./give-feedback');
const concludeFeedback = require('./conclude-feedback');
const checkFeedback = require('./check-feedback');

module.exports = {
  giveFeedback,
  checkFeedback,
  concludeFeedback,
};
